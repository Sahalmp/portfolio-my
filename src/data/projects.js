// Single source of truth: all data lives in portfolio.json
import data from './portfolio.json'

export const projects = data.projects
export const skills = data.skills
export const skillOrbs = data.skillOrbs
export const componentLibrary = data.componentLibrary
export const experience = data.experience

export default data
